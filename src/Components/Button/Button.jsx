import React from 'react'

const Button = (props) => {
  return (
  <button className="main__button-now">
    <img className="icon-button" alt="" src={props.icon} />assistir agora
  </button>
  )
}

export default Button
